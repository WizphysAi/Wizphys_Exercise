using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RotationOfQAScreen : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Screen.orientation = ScreenOrientation.Portrait; //To change Camera Orination in Portrait!!
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
